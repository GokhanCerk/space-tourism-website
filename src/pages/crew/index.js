import React from 'react'
import styles from './index.module.css';

const Crew = () => {
  const { main } = styles;

  return (
   <main className={main}>
     crew
   </main>
  )
}

export default Crew